# TP+路由软件 TDD 实现计划

**目标：** 实现一个硬件无关、OS无关的C99嵌入式TP+路由库，支持多拓扑、长数据拆帧、ACK可靠性
**行为规格：** `docs/specs/2026-06-08-tp-route.feature`
**技术方案：** `docs/specs/2026-06-08-tp-route-design.md`

---

### Task 0: 项目脚手架

- [ ] 创建目录结构

```
src/
  comm_route.h        # 公共 API 头文件
  comm_route.c        # 实现
  comm_route_internal.h  # 内部类型和宏
tests/
  test_main.c         # Unity test runner
  test_instance.c     # 实例管理测试
CMakeLists.txt        # 构建配置（含测试 target）
```

- [ ] 配置 CMake + Unity 测试框架

```cmake
cmake_minimum_required(VERSION 3.14)
project(comm_route C)
set(CMAKE_C_STANDARD 99)

# 库
add_library(comm_route src/comm_route.c)
target_include_directories(comm_route PUBLIC src/)

# Unity (FetchContent)
include(FetchContent)
FetchContent_Declare(unity
  GIT_REPOSITORY https://github.com/ThrowTheSwitch/Unity.git
  GIT_TAG v2.5.2)
FetchContent_MakeAvailable(unity)

# 测试
enable_testing()
add_executable(test_all tests/test_main.c tests/test_instance.c)
target_link_libraries(test_all comm_route unity)
add_test(NAME unit_tests COMMAND test_all)
```

- [ ] 创建空骨架让构建通过

```c
// src/comm_route.h
#ifndef COMM_ROUTE_H
#define COMM_ROUTE_H
#include <stdint.h>
#include <stddef.h>
#endif

// src/comm_route.c
#include "comm_route.h"
```

- [ ] 验证构建和空测试可运行

```bash
mkdir -p build && cd build && cmake .. && make && ctest
```

- [ ] Commit

```bash
git commit -m "chore: project scaffold with CMake + Unity"
```

---

### Task 1: 创建实例并绑定静态内存池

**Scenario:**
```gherkin
Given 用户提供一块 1024 字节的 buffer
And 用户提供实例配置（本机地址=0x01, MTU=64, 帧间隔=5ms, 最大重传=3, ACK超时=100ms）
When 用户调用初始化函数创建实例
Then 实例创建成功
And 实例的内存全部来自用户提供的 buffer
```

**文件：**
- 测试: `tests/test_instance.c`
- 实现: `src/comm_route.h`, `src/comm_route.c`

- [ ] **Red: 编写失败测试**

```c
void test_create_instance_with_static_buffer(void) {
    uint8_t buffer[1024];
    cr_config_t cfg = {
        .local_addr = 0x01,
        .mtu = 64,
        .frame_interval_ms = 5,
        .max_retries = 3,
        .ack_timeout_ms = 100,
        .ack_enabled = 1,
        .ack_mode = CR_ACK_MODE_REPLY,
        .default_ttl = 3,
        .tx_queue_depth = 4,
        .rx_assem_count = 2,
        .dedup_table_size = 16,
        .rx_assem_timeout_ms = 1000,
        .route_table = NULL,
        .route_count = 0,
    };
    cr_instance_t inst;
    int ret = cr_init(&inst, &cfg, buffer, sizeof(buffer));
    TEST_ASSERT_EQUAL_INT(0, ret);
}
```

- [ ] **Red: 确认测试失败**

```bash
cd build && cmake .. && make && ctest --output-on-failure
```
预期: FAIL（cr_config_t / cr_instance_t / cr_init 未定义）

- [ ] **Green: 最小实现**

在 `comm_route.h` 中定义 `cr_config_t`、`cr_ack_mode_t`、`cr_route_entry_t`、`cr_instance_t`（不透明结构体）。
在 `comm_route.c` 中实现 `cr_calc_buffer_size()` 和 `cr_init()`：
- 计算各区域所需大小
- 验证 buffer_size 够用
- 将 buffer 内部划分给各区域
- 将配置拷贝到实例结构体内

- [ ] **Green: 确认测试通过**

```bash
cd build && cmake .. && make && ctest --output-on-failure
```
预期: PASS

- [ ] **Refactor**

运行全部测试确认不破坏。

- [ ] **Commit**

```bash
git commit -m "feat: instance creation with static memory pool"
```

---

### Task 2: Buffer 不足时初始化失败

**Scenario:**
```gherkin
Given 用户提供一块 32 字节的 buffer（小于所需最小值）
When 用户调用 cr_init 初始化实例
Then 返回 -3（buffer 不足）
```

**文件：**
- 测试: `tests/test_instance.c`
- 实现: `src/comm_route.c`

- [ ] **Red: 编写失败测试**

```c
void test_init_fails_with_insufficient_buffer(void) {
    uint8_t buffer[32];
    cr_config_t cfg = {
        .local_addr = 0x01, .mtu = 64,
        .tx_queue_depth = 4, .rx_assem_count = 2,
        .dedup_table_size = 16,
    };
    cr_instance_t inst;
    int ret = cr_init(&inst, &cfg, buffer, sizeof(buffer));
    TEST_ASSERT_EQUAL_INT(-3, ret);
}
```

- [ ] **Red: 确认测试失败**

预期: FAIL（当前 cr_init 可能不检查 buffer 大小）

- [ ] **Green: 最小实现**

在 `cr_init` 开头加入 `if (buffer_size < cr_calc_buffer_size(cfg)) return -3;`

- [ ] **Green: 确认测试通过**
- [ ] **Refactor**
- [ ] **Commit**

```bash
git commit -m "feat: reject init when buffer too small"
```

---

### Task 3: 多实例独立运行

**Scenario:**
```gherkin
Given 已创建实例A（地址=0x01, buffer=1024字节）
And 已创建实例B（地址=0x02, buffer=512字节）
When 实例A发送数据
Then 实例A不会使用实例B的内存
And 实例B的状态不受影响
```

**文件：**
- 测试: `tests/test_instance.c`
- 实现: 无新实现（验证隔离性）

- [ ] **Red: 编写失败测试**

```c
static int send_count_a;
static int mock_send_a(void *ctx, const uint8_t *data, uint16_t len) {
    send_count_a++;
    return 0;
}
static uint32_t mock_tick_val = 0;
static uint32_t mock_get_tick(void) { return mock_tick_val; }

void test_multi_instance_isolation(void) {
    uint8_t buf_a[1024], buf_b[512];
    cr_config_t cfg_a = { .local_addr = 0x01, .mtu = 8, .tx_queue_depth = 2,
                          .rx_assem_count = 1, .dedup_table_size = 8,
                          .frame_interval_ms = 0, .max_retries = 3,
                          .ack_timeout_ms = 100, .ack_enabled = 0,
                          .ack_mode = CR_ACK_MODE_REPLY, .default_ttl = 3,
                          .rx_assem_timeout_ms = 1000,
                          .route_table = NULL, .route_count = 0 };
    cr_config_t cfg_b = { .local_addr = 0x02, .mtu = 8, .tx_queue_depth = 2,
                          .rx_assem_count = 1, .dedup_table_size = 8,
                          .frame_interval_ms = 0, .max_retries = 3,
                          .ack_timeout_ms = 100, .ack_enabled = 0,
                          .ack_mode = CR_ACK_MODE_REPLY, .default_ttl = 3,
                          .rx_assem_timeout_ms = 1000,
                          .route_table = NULL, .route_count = 0 };
    cr_instance_t inst_a, inst_b;
    TEST_ASSERT_EQUAL_INT(0, cr_init(&inst_a, &cfg_a, buf_a, sizeof(buf_a)));
    TEST_ASSERT_EQUAL_INT(0, cr_init(&inst_b, &cfg_b, buf_b, sizeof(buf_b)));

    // 在B的buffer末尾写标记
    buf_b[510] = 0xDE;
    buf_b[511] = 0xAD;

    // 对A操作
    cr_hal_t hal_a = { .send = mock_send_a, .get_tick_ms = mock_get_tick, .hw_ctx = NULL };
    cr_set_hal(&inst_a, &hal_a);
    uint8_t data[] = {1, 2, 3};
    cr_send(&inst_a, 0x03, 0, data, 3, NULL, NULL);
    cr_poll(&inst_a);

    // 验证B的buffer末尾标记未被修改
    TEST_ASSERT_EQUAL_UINT8(0xDE, buf_b[510]);
    TEST_ASSERT_EQUAL_UINT8(0xAD, buf_b[511]);
}
```

- [ ] **Red: 确认测试失败**
- [ ] **Green: 最小实现**

实例结构体内所有指针都指向自己的 buffer 范围内。验证通过说明隔离成立。

- [ ] **Green: 确认测试通过**
- [ ] **Refactor**

运行全部测试确认不破坏。

- [ ] **Commit**

```bash
git commit -m "test: verify multi-instance memory isolation"
```

---

### Task 4: 单播发送单帧

**Scenario:**
```gherkin
Given 实例配置本机地址=0x01
And 路由表配置：目标0x03, 下一跳=0x02
When 用户发送单帧数据 payload="HELLO" 目标=0x03
Then 帧通过硬件接口发出
And 帧头包含源地址=0x01, 目标地址=0x03
```

**文件：**
- 测试: `tests/test_send.c`
- 实现: `src/comm_route.c`

- [ ] **Red: 编写失败测试**

```c
static uint8_t sent_buf[64];
static uint16_t sent_len;

static int mock_send(void *ctx, const uint8_t *data, uint16_t len) {
    memcpy(sent_buf, data, len);
    sent_len = len;
    return 0;
}

static uint32_t mock_tick = 0;
static uint32_t mock_get_tick(void) { return mock_tick; }

void test_unicast_send_single_frame(void) {
    uint8_t buffer[1024];
    cr_route_entry_t routes[] = { {.dest = 0x03, .next_hop = 0x02} };
    cr_config_t cfg = {
        .local_addr = 0x01, .mtu = 64, .frame_interval_ms = 0,
        .ack_enabled = 0, .default_ttl = 3,
        .tx_queue_depth = 4, .rx_assem_count = 2, .dedup_table_size = 16,
        .route_table = routes, .route_count = 1,
    };
    cr_instance_t inst;
    cr_init(&inst, &cfg, buffer, sizeof(buffer));

    cr_hal_t hal = { .send = mock_send, .get_tick_ms = mock_get_tick, .hw_ctx = NULL };
    cr_set_hal(&inst, &hal);

    uint8_t payload[] = "HELLO";
    int ret = cr_send(&inst, 0x03, 0, payload, 5, NULL, NULL);
    TEST_ASSERT_EQUAL_INT(0, ret);

    cr_poll(&inst);

    // 验证帧头: DST=0x03, SRC=0x01
    TEST_ASSERT_EQUAL_UINT8(0x03, sent_buf[0]);  // DST
    TEST_ASSERT_EQUAL_UINT8(0x01, sent_buf[1]);  // SRC
    // CTL: ACK=0, 广播=0, 分片=0, 末帧=0, bizid=0 → 0x00
    TEST_ASSERT_EQUAL_UINT8(0x00, sent_buf[2]);
    // PAYLOAD
    TEST_ASSERT_EQUAL_UINT8_ARRAY(payload, &sent_buf[5], 5);
}
```

- [ ] **Red: 确认测试失败**

预期: FAIL（cr_send / cr_set_hal / cr_poll 未实现）

- [ ] **Green: 最小实现**

实现：
- `cr_set_hal()`：保存 HAL 指针到实例
- `cr_send()`：创建 TX 任务放入队列
- `cr_poll()`：从队列取任务，构造帧头，查路由表，调用 HAL send

- [ ] **Green: 确认测试通过**
- [ ] **Refactor**
- [ ] **Commit**

```bash
git commit -m "feat: unicast single frame send with routing"
```

---

### Task 5: 发送队列满时返回错误

**Scenario:**
```gherkin
Given 实例配置 tx_queue_depth=2
And 队列中已有 2 个待发送任务
When 用户再次调用 cr_send 提交发送请求
Then 返回 -1（队列满）
```

**文件：**
- 测试: `tests/test_send.c`
- 实现: `src/comm_route.c`

- [ ] **Red: 编写失败测试**

```c
void test_send_queue_full(void) {
    uint8_t buffer[1024];
    cr_config_t cfg = { .local_addr = 0x01, .mtu = 64, .tx_queue_depth = 2,
                        .ack_enabled = 0, .ack_mode = CR_ACK_MODE_REPLY,
                        .rx_assem_count = 1, .dedup_table_size = 8,
                        .frame_interval_ms = 0, .max_retries = 3,
                        .ack_timeout_ms = 100, .default_ttl = 3,
                        .rx_assem_timeout_ms = 1000,
                        .route_table = NULL, .route_count = 0 };
    cr_instance_t inst;
    cr_init(&inst, &cfg, buffer, sizeof(buffer));
    cr_hal_t hal = { .send = mock_send, .get_tick_ms = mock_get_tick, .hw_ctx = NULL };
    cr_set_hal(&inst, &hal);

    uint8_t data[] = {1, 2, 3};
    TEST_ASSERT_EQUAL_INT(0, cr_send(&inst, 0x02, 0, data, 3, NULL, NULL));
    TEST_ASSERT_EQUAL_INT(0, cr_send(&inst, 0x03, 0, data, 3, NULL, NULL));
    TEST_ASSERT_EQUAL_INT(-1, cr_send(&inst, 0x04, 0, data, 3, NULL, NULL));
}
```

- [ ] **Red: 确认测试失败**
- [ ] **Green: 最小实现**

在 `cr_send` 中检查队列是否已满，满则返回 -1。

- [ ] **Green: 确认测试通过**
- [ ] **Refactor**
- [ ] **Commit**

```bash
git commit -m "feat: return -1 when tx queue full"
```

---

### Task 6: 广播发送单帧

**Scenario:**
```gherkin
Given 实例配置本机地址=0x01, 默认TTL=3
When 用户发送广播帧 payload="BCAST"
Then 帧通过硬件接口发出
And 帧头目标地址为广播地址(0xFF), TTL=3, 源地址=0x01
```

**文件：**
- 测试: `tests/test_broadcast.c`
- 实现: `src/comm_route.c`

- [ ] **Red: 编写失败测试**

```c
void test_broadcast_single_frame(void) {
    uint8_t buffer[1024];
    cr_config_t cfg = { .local_addr = 0x01, .mtu = 64, .tx_queue_depth = 4,
                        .ack_enabled = 0, .ack_mode = CR_ACK_MODE_REPLY,
                        .rx_assem_count = 2, .dedup_table_size = 16,
                        .frame_interval_ms = 0, .max_retries = 3,
                        .ack_timeout_ms = 100, .default_ttl = 3,
                        .rx_assem_timeout_ms = 1000,
                        .route_table = NULL, .route_count = 0 };
    cr_instance_t inst;
    cr_init(&inst, &cfg, buffer, sizeof(buffer));
    cr_hal_t hal = { .send = mock_send, .get_tick_ms = mock_get_tick, .hw_ctx = NULL };
    cr_set_hal(&inst, &hal);

    sent_len = 0;
    uint8_t payload[] = "BCAST";
    int ret = cr_broadcast(&inst, 0, payload, 5);
    TEST_ASSERT_EQUAL_INT(0, ret);

    cr_poll(&inst);

    TEST_ASSERT_EQUAL_UINT8(0xFF, sent_buf[0]);  // DST = broadcast
    TEST_ASSERT_EQUAL_UINT8(0x01, sent_buf[1]);  // SRC
    // CTL: bit6=1(广播) → 0x40
    TEST_ASSERT_EQUAL_UINT8(0x40, sent_buf[2]);
    TEST_ASSERT_EQUAL_UINT8(3, sent_buf[4]);     // TTL=3
}
```

- [ ] **Red: 确认测试失败**
- [ ] **Green: 最小实现**

实现 `cr_broadcast()`：放入广播槽（独立于单播队列），`cr_poll` 中独立处理广播槽。

- [ ] **Green: 确认测试通过**
- [ ] **Refactor**
- [ ] **Commit**

```bash
git commit -m "feat: broadcast single frame send"
```

---

### Task 7: 广播数据超过 MTU 时返回错误

**Scenario:**
```gherkin
Given 实例配置 MTU=8
When 用户调用 cr_broadcast 发送 16 字节数据
Then 返回 -2（参数错误，广播仅支持单帧）
```

**文件：**
- 测试: `tests/test_broadcast.c`
- 实现: `src/comm_route.c`

- [ ] **Red: 编写失败测试**

```c
void test_broadcast_exceeds_mtu(void) {
    // setup instance with mtu=8
    uint8_t data[16] = {0};
    int ret = cr_broadcast(&inst, 0, data, 16);
    TEST_ASSERT_EQUAL_INT(-2, ret);
}
```

- [ ] **Red: 确认测试失败**
- [ ] **Green: 最小实现**

在 `cr_broadcast` 开头: `if (len > inst->cfg.mtu) return -2;`

- [ ] **Green: 确认测试通过**
- [ ] **Refactor**
- [ ] **Commit**

```bash
git commit -m "feat: reject broadcast exceeding MTU"
```

---

### Task 8: 收到发给自己的单帧

**Scenario:**
```gherkin
Given 实例配置本机地址=0x02
When 从硬件接口收到一帧（源=0x01, 目标=0x02）
Then 实例将该帧交给上层处理（不转发）
```

**文件：**
- 测试: `tests/test_recv.c`
- 实现: `src/comm_route.c`

- [ ] **Red: 编写失败测试**

```c
static uint8_t recv_src;
static uint8_t recv_biz_id;
static uint8_t recv_data[64];
static uint16_t recv_len;
static int recv_called;

static void mock_recv_cb(cr_instance_t *inst, uint8_t src, uint8_t biz_id,
                         const uint8_t *data, uint16_t len, void *ctx) {
    recv_src = src;
    recv_biz_id = biz_id;
    memcpy(recv_data, data, len);
    recv_len = len;
    recv_called = 1;
}

void test_receive_unicast_single_frame(void) {
    // setup instance (local_addr=0x02), set recv callback
    cr_set_recv_callback(&inst, mock_recv_cb, NULL);

    // 构造帧: DST=0x02, SRC=0x01, CTL=0x00, SEQ=0, TTL=3, PAYLOAD="HI"
    uint8_t frame[] = {0x02, 0x01, 0x00, 0x00, 0x03, 'H', 'I'};
    recv_called = 0;
    cr_feed_frame(&inst, frame, sizeof(frame));

    TEST_ASSERT_EQUAL_INT(1, recv_called);
    TEST_ASSERT_EQUAL_UINT8(0x01, recv_src);
    TEST_ASSERT_EQUAL_UINT16(2, recv_len);
    TEST_ASSERT_EQUAL_UINT8_ARRAY("HI", recv_data, 2);
}
```

- [ ] **Red: 确认测试失败**
- [ ] **Green: 最小实现**

实现 `cr_set_recv_callback()` 和 `cr_feed_frame()` 的 DST==本机 分支。

- [ ] **Green: 确认测试通过**
- [ ] **Refactor**
- [ ] **Commit**

```bash
git commit -m "feat: receive unicast single frame with callback"
```

---

### Task 9: 中间节点透传转发

**Scenario:**
```gherkin
# 覆盖：单帧发送与路由 - "中间节点透传转发"
# 同时覆盖：拓扑支持 - "链式拓扑 - 逐跳转发"
Given 实例配置本机地址=0x02
And 路由表配置：目标0x03, 下一跳=0x03（直连）
When 从硬件接口收到一帧（源=0x01, 目标=0x03）
Then 实例查路由表找到下一跳=0x03
And 将该帧原样转发到下一跳
```

**文件：**
- 测试: `tests/test_recv.c`
- 实现: `src/comm_route.c`

- [ ] **Red: 编写失败测试**

```c
void test_forward_frame_to_next_hop(void) {
    // setup instance (local_addr=0x02, route: 0x03→直连)
    sent_len = 0;
    uint8_t frame[] = {0x03, 0x01, 0x00, 0x00, 0x03, 'D', 'A', 'T', 'A'};
    cr_feed_frame(&inst, frame, sizeof(frame));

    // 验证帧被原样转发
    TEST_ASSERT_EQUAL_UINT16(sizeof(frame), sent_len);
    TEST_ASSERT_EQUAL_UINT8_ARRAY(frame, sent_buf, sizeof(frame));
}
```

- [ ] **Red: 确认测试失败**
- [ ] **Green: 最小实现**

在 `cr_feed_frame` 中添加 "其他" 分支：查路由表，找到则调用 HAL send 原样转发。

- [ ] **Green: 确认测试通过**
- [ ] **Refactor**
- [ ] **Commit**

```bash
git commit -m "feat: forward frames via route table lookup"
```

---

### Task 10: 无路由则丢弃

**Scenario:**
```gherkin
Given 节点 B 地址=0x02
And 路由表中无到 0x05 的条目
When B 收到一帧（源=0x01, 目标=0x05）
Then B 丢弃该帧（无路由，不转发）
```

**文件：**
- 测试: `tests/test_recv.c`
- 实现: 已有逻辑（验证）

- [ ] **Red: 编写失败测试**

```c
void test_drop_frame_no_route(void) {
    // setup: route table only has 0x03
    sent_len = 0;
    uint8_t frame[] = {0x05, 0x01, 0x00, 0x00, 0x03, 'X'};
    cr_feed_frame(&inst, frame, sizeof(frame));

    TEST_ASSERT_EQUAL_UINT16(0, sent_len);  // 未调用 send
}
```

- [ ] **Red: 确认测试失败**
- [ ] **Green: 最小实现**

路由查找失败时直接 return（不调用 send）。

- [ ] **Green: 确认测试通过**
- [ ] **Refactor**
- [ ] **Commit**

```bash
git commit -m "feat: drop frames with no matching route"
```

---

### Task 11: 接收广播帧并转发（TTL 递减）

**Scenario:**
```gherkin
Given 实例配置本机地址=0x02
When 收到广播帧（源=0x01, TTL=2）
Then 实例将该帧交给上层处理
And TTL 减 1 后转发（TTL=1）
And 记录该帧的（源地址+序号）用于去重
```

**文件：**
- 测试: `tests/test_broadcast.c`
- 实现: `src/comm_route.c`

- [ ] **Red: 编写失败测试**

```c
void test_receive_broadcast_and_forward(void) {
    // setup instance (local_addr=0x02)
    cr_set_recv_callback(&inst, mock_recv_cb, NULL);
    recv_called = 0;
    sent_len = 0;

    // CTL: bit6=1 (广播) → 0x40, SEQ=7, TTL=2
    uint8_t frame[] = {0xFF, 0x01, 0x40, 0x07, 0x02, 'B'};
    cr_feed_frame(&inst, frame, sizeof(frame));

    // 交给上层
    TEST_ASSERT_EQUAL_INT(1, recv_called);
    // 转发且 TTL 递减
    TEST_ASSERT_TRUE(sent_len > 0);
    TEST_ASSERT_EQUAL_UINT8(0x01, sent_buf[4]);  // TTL=2-1=1
}
```

- [ ] **Red: 确认测试失败**
- [ ] **Green: 最小实现**

在 `cr_feed_frame` 广播分支：触发回调 → 记录去重表 → TTL > 0 时递减并转发。

- [ ] **Green: 确认测试通过**
- [ ] **Refactor**
- [ ] **Commit**

```bash
git commit -m "feat: broadcast receive, forward with TTL decrement"
```

---

### Task 12: 广播 TTL 耗尽停止转发

**Scenario:**
```gherkin
Given 实例配置本机地址=0x03
When 收到广播帧（源=0x01, TTL=0）
Then 实例将该帧交给上层处理
And 不再转发该帧
```

**文件：**
- 测试: `tests/test_broadcast.c`
- 实现: 已有逻辑（验证 TTL=0 时不转发）

- [ ] **Red: 编写失败测试**

```c
void test_broadcast_ttl_zero_no_forward(void) {
    recv_called = 0;
    sent_len = 0;
    // TTL=0
    uint8_t frame[] = {0xFF, 0x01, 0x40, 0x08, 0x00, 'X'};
    cr_feed_frame(&inst, frame, sizeof(frame));

    TEST_ASSERT_EQUAL_INT(1, recv_called);   // 仍处理
    TEST_ASSERT_EQUAL_UINT16(0, sent_len);   // 不转发
}
```

- [ ] **Red: 确认测试失败**
- [ ] **Green: 确认测试通过**（TTL==0 时不进入转发，已在 Task 11 实现）
- [ ] **Refactor**
- [ ] **Commit**

```bash
git commit -m "test: verify broadcast TTL=0 stops forwarding"
```

---

### Task 13: 广播去重

**Scenario:**
```gherkin
Given 实例配置本机地址=0x02
And 已记录来自 0x01 的序号=5 的广播帧
When 再次收到同一广播帧（源=0x01, 序号=5）
Then 丢弃该帧（不处理、不转发）
```

**文件：**
- 测试: `tests/test_broadcast.c`
- 实现: `src/comm_route.c`

- [ ] **Red: 编写失败测试**

```c
void test_broadcast_dedup(void) {
    recv_called = 0;
    // 第一次：正常处理
    uint8_t frame[] = {0xFF, 0x01, 0x40, 0x05, 0x02, 'D'};
    cr_feed_frame(&inst, frame, sizeof(frame));
    TEST_ASSERT_EQUAL_INT(1, recv_called);

    // 第二次：重复，丢弃
    recv_called = 0;
    sent_len = 0;
    cr_feed_frame(&inst, frame, sizeof(frame));
    TEST_ASSERT_EQUAL_INT(0, recv_called);
    TEST_ASSERT_EQUAL_UINT16(0, sent_len);
}
```

- [ ] **Red: 确认测试失败**
- [ ] **Green: 最小实现**

去重表环形写入，查找时遍历比对 (SRC + SEQ)。

- [ ] **Green: 确认测试通过**
- [ ] **Refactor**
- [ ] **Commit**

```bash
git commit -m "feat: broadcast deduplication"
```

---

### Task 14: 长数据拆帧发送

**Scenario:**
```gherkin
Given 实例配置 MTU=8, 帧间隔=10ms
When 用户提交 20 字节的长数据发送请求（目标=0x03）
Then 数据被拆为 3 帧（8+8+4 字节）
```

**文件：**
- 测试: `tests/test_send.c`
- 实现: `src/comm_route.c`

- [ ] **Red: 编写失败测试**

```c
static int send_count;
static uint8_t send_history[10][64];
static uint16_t send_history_len[10];

static int mock_send_record(void *ctx, const uint8_t *data, uint16_t len) {
    memcpy(send_history[send_count], data, len);
    send_history_len[send_count] = len;
    send_count++;
    return 0;
}

void test_long_data_segmentation(void) {
    // setup: MTU=8, frame_interval=0(简化), ack_enabled=0
    send_count = 0;
    uint8_t data[20];
    for (int i = 0; i < 20; i++) data[i] = i;

    cr_send(&inst, 0x03, 0, data, 20, NULL, NULL);

    // 驱动3次poll发出3帧
    cr_poll(&inst); cr_poll(&inst); cr_poll(&inst);

    TEST_ASSERT_EQUAL_INT(3, send_count);
    // 帧1: 5字节头 + 8字节负载 = 13
    TEST_ASSERT_EQUAL_UINT16(13, send_history_len[0]);
    // 帧2: 5 + 8 = 13
    TEST_ASSERT_EQUAL_UINT16(13, send_history_len[1]);
    // 帧3: 5 + 4 = 9
    TEST_ASSERT_EQUAL_UINT16(9, send_history_len[2]);

    // 验证分片标记: 帧1和帧2 CTL.bit5=1, 帧3 CTL.bit5=1 + bit4=1(末帧)
    TEST_ASSERT_BITS(0x20, 0x20, send_history[0][2]);  // 分片=1
    TEST_ASSERT_BITS(0x10, 0x00, send_history[0][2]);  // 末帧=0
    TEST_ASSERT_BITS(0x30, 0x30, send_history[2][2]);  // 分片=1, 末帧=1
}
```

- [ ] **Red: 确认测试失败**
- [ ] **Green: 最小实现**

修改 `cr_send`：len > MTU 时标记为长数据任务。
修改 `cr_poll`：长数据任务按偏移量切分 payload，构造分片帧头（CTL.bit5=1，末帧 CTL.bit4=1），递增 SEQ。

- [ ] **Green: 确认测试通过**
- [ ] **Refactor**
- [ ] **Commit**

```bash
git commit -m "feat: long data segmentation into multiple frames"
```

---

### Task 15: Poll 中逐帧发送（帧间隔）

**Scenario:**
```gherkin
Given 有一个待发送的 3 帧长数据任务
And 帧间隔=10ms
When 第一次调用 poll 且距上次发送≥10ms → 发送第 1 帧
When 再次调用 poll 且距上次发送≥10ms → 发送第 2 帧
```

**文件：**
- 测试: `tests/test_send.c`
- 实现: `src/comm_route.c`

- [ ] **Red: 编写失败测试**

```c
void test_frame_interval_pacing(void) {
    // setup: MTU=8, frame_interval=10ms, ack_enabled=0
    send_count = 0;
    uint8_t data[20] = {0};
    cr_send(&inst, 0x03, 0, data, 20, NULL, NULL);

    mock_tick = 0;
    cr_poll(&inst);
    TEST_ASSERT_EQUAL_INT(1, send_count);  // 第1帧

    mock_tick = 5;  // 才过5ms
    cr_poll(&inst);
    TEST_ASSERT_EQUAL_INT(1, send_count);  // 未发送

    mock_tick = 10;  // 够10ms
    cr_poll(&inst);
    TEST_ASSERT_EQUAL_INT(2, send_count);  // 第2帧
}
```

- [ ] **Red: 确认测试失败**
- [ ] **Green: 最小实现**

在 SENDING 状态检查 `get_tick_ms() - last_tick >= frame_interval_ms`，不满足则跳过。

- [ ] **Green: 确认测试通过**
- [ ] **Refactor**
- [ ] **Commit**

```bash
git commit -m "feat: frame interval pacing in poll"
```

---

### Task 16: ACK 关闭 - 发完即忘

**Scenario:**
```gherkin
Given 实例配置 ACK=关闭
When 发送一帧到目标 0x03
Then 帧发出后立即标记为发送成功（不等待ACK）
```

**文件：**
- 测试: `tests/test_ack.c`
- 实现: `src/comm_route.c`

- [ ] **Red: 编写失败测试**

```c
static uint8_t complete_status;
static int complete_called;
static void mock_on_complete(uint8_t status, void *ctx) {
    complete_status = status;
    complete_called = 1;
}

void test_ack_disabled_fire_and_forget(void) {
    // setup: ack_enabled=0
    complete_called = 0;
    uint8_t data[] = {1, 2, 3};
    cr_send(&inst, 0x03, 0, data, 3, mock_on_complete, NULL);
    cr_poll(&inst);

    TEST_ASSERT_EQUAL_INT(1, complete_called);
    TEST_ASSERT_EQUAL_UINT8(0, complete_status);  // 0=成功
}
```

- [ ] **Red: 确认测试失败**
- [ ] **Green: 最小实现**

ACK 关闭时，发送后直接标记成功并触发回调。

- [ ] **Green: 确认测试通过**
- [ ] **Refactor**
- [ ] **Commit**

```bash
git commit -m "feat: fire-and-forget when ACK disabled"
```

---

### Task 17: 发送完成回调（ACK 关闭，长数据）

**Scenario:**
```gherkin
Given 有一个 3 帧长数据任务且 ACK=关闭
When 所有帧已发出
Then 触发发送完成回调，通知用户该长数据发送完成
```

**文件：**
- 测试: `tests/test_send.c`
- 实现: `src/comm_route.c`

- [ ] **Red: 编写失败测试**

```c
void test_long_data_complete_callback_ack_off(void) {
    // setup: MTU=8, ack_enabled=0, frame_interval=0
    complete_called = 0;
    uint8_t data[20] = {0};
    cr_send(&inst, 0x03, 0, data, 20, mock_on_complete, NULL);

    cr_poll(&inst); cr_poll(&inst);
    TEST_ASSERT_EQUAL_INT(0, complete_called);  // 还没发完

    cr_poll(&inst);  // 第3帧（最后）
    TEST_ASSERT_EQUAL_INT(1, complete_called);
    TEST_ASSERT_EQUAL_UINT8(0, complete_status);  // 成功
}
```

- [ ] **Red: 确认测试失败**
- [ ] **Green: 最小实现**

长数据最后一帧发完后触发回调。

- [ ] **Green: 确认测试通过**
- [ ] **Refactor**
- [ ] **Commit**

```bash
git commit -m "feat: long data complete callback (ACK off)"
```

---

### Task 18: ACK 开启 - 正常确认（目标回复方式）

**Scenario:**
```gherkin
Given 实例配置 ACK=开启, 超时=100ms, 最大重传=3
When 发送一帧到目标 0x03
And 在 100ms 内收到来自 0x03 的 ACK
Then 该帧标记为发送成功
```

**文件：**
- 测试: `tests/test_ack.c`
- 实现: `src/comm_route.c`

- [ ] **Red: 编写失败测试**

```c
void test_ack_reply_normal(void) {
    // setup: ack_enabled=1, ack_mode=CR_ACK_MODE_REPLY, timeout=100ms
    complete_called = 0;
    uint8_t data[] = {1, 2, 3};
    cr_send(&inst, 0x03, 0, data, 3, mock_on_complete, NULL);

    cr_poll(&inst);  // 发送帧 (SEQ 应为某个值)

    // 构造 ACK 帧: DST=本机(0x01), SRC=0x03, CTL.bit7=1, SEQ=与发送的SEQ匹配
    uint8_t ack_frame[] = {0x01, 0x03, 0x80, sent_buf[3], 0x03};
    cr_feed_frame(&inst, ack_frame, sizeof(ack_frame));

    TEST_ASSERT_EQUAL_INT(1, complete_called);
    TEST_ASSERT_EQUAL_UINT8(0, complete_status);
}
```

- [ ] **Red: 确认测试失败**
- [ ] **Green: 最小实现**

实现 WAIT_ACK 状态：
- `cr_poll` 发送后进入 WAIT_ACK
- `cr_feed_frame` 中 DST==本机 && CTL.bit7==1 → 匹配活跃任务 SEQ → 标记成功

- [ ] **Green: 确认测试通过**
- [ ] **Refactor**
- [ ] **Commit**

```bash
git commit -m "feat: ACK reply mode - normal confirmation"
```

---

### Task 19: ACK 开启 - 超时重传

**Scenario:**
```gherkin
Given 实例配置 ACK=开启, 超时=100ms, 最大重传=3
When 发送一帧到目标 0x03
And 100ms 内未收到 ACK
Then 自动重传该帧 And 重传计数+1
```

**文件：**
- 测试: `tests/test_ack.c`
- 实现: `src/comm_route.c`

- [ ] **Red: 编写失败测试**

```c
void test_ack_timeout_retransmit(void) {
    // setup: ack_enabled=1, timeout=100ms
    send_count = 0;
    uint8_t data[] = {1, 2, 3};
    cr_send(&inst, 0x03, 0, data, 3, mock_on_complete, NULL);

    mock_tick = 0;
    cr_poll(&inst);  // 首次发送
    TEST_ASSERT_EQUAL_INT(1, send_count);

    mock_tick = 101;  // 超时
    cr_poll(&inst);   // 重传
    TEST_ASSERT_EQUAL_INT(2, send_count);
    // 重传帧内容应与首次相同
    TEST_ASSERT_EQUAL_UINT8_ARRAY(send_history[0], send_history[1], send_history_len[0]);
}
```

- [ ] **Red: 确认测试失败**
- [ ] **Green: 最小实现**

WAIT_ACK 状态中：`get_tick_ms() - last_tick >= ack_timeout_ms` 且未达重传上限 → 重发帧，retries++。

- [ ] **Green: 确认测试通过**
- [ ] **Refactor**
- [ ] **Commit**

```bash
git commit -m "feat: ACK timeout retransmission"
```

---

### Task 20: ACK 开启 - 达到最大重传次数

**Scenario:**
```gherkin
Given 实例配置 ACK=开启, 超时=100ms, 最大重传=3
When 一帧已重传 3 次仍未收到 ACK
Then 该帧标记为发送失败
And 通知上层（如果是长数据的一部分，整个长数据标记失败）
```

**文件：**
- 测试: `tests/test_ack.c`
- 实现: `src/comm_route.c`

- [ ] **Red: 编写失败测试**

```c
void test_ack_max_retries_fail(void) {
    // setup: ack_enabled=1, timeout=100ms, max_retries=3
    complete_called = 0;
    uint8_t data[] = {1, 2, 3};
    cr_send(&inst, 0x03, 0, data, 3, mock_on_complete, NULL);

    // 首次 + 3次重传 = 4次发送，之后失败
    mock_tick = 0;   cr_poll(&inst);  // 首发
    mock_tick = 101; cr_poll(&inst);  // 重传1
    mock_tick = 202; cr_poll(&inst);  // 重传2
    mock_tick = 303; cr_poll(&inst);  // 重传3
    mock_tick = 404; cr_poll(&inst);  // 判定失败

    TEST_ASSERT_EQUAL_INT(1, complete_called);
    TEST_ASSERT_NOT_EQUAL(0, complete_status);  // 非0=失败
}
```

- [ ] **Red: 确认测试失败**
- [ ] **Green: 最小实现**

retries >= max_retries 时标记 FAILED，触发回调（status=非0）。

- [ ] **Green: 确认测试通过**
- [ ] **Refactor**
- [ ] **Commit**

```bash
git commit -m "feat: fail after max retries exhausted"
```

---

### Task 21: 发送完成回调（ACK 开启，长数据）

**Scenario:**
```gherkin
Given 有一个 3 帧长数据任务且 ACK=开启
When 所有帧发送完成且 ACK 全部确认
Then 触发发送完成回调，通知用户该长数据发送成功
```

**文件：**
- 测试: `tests/test_ack.c`
- 实现: `src/comm_route.c`

- [ ] **Red: 编写失败测试**

```c
void test_long_data_complete_with_ack(void) {
    // setup: MTU=8, ack_enabled=1, timeout=100, frame_interval=0
    complete_called = 0;
    uint8_t data[20] = {0};
    cr_send(&inst, 0x03, 0, data, 20, mock_on_complete, NULL);

    for (int i = 0; i < 3; i++) {
        cr_poll(&inst);  // 发送帧i
        // 模拟收到 ACK
        uint8_t ack[] = {0x01, 0x03, 0x80, sent_buf[3], 0x03};
        cr_feed_frame(&inst, ack, sizeof(ack));
    }

    TEST_ASSERT_EQUAL_INT(1, complete_called);
    TEST_ASSERT_EQUAL_UINT8(0, complete_status);
}
```

- [ ] **Red: 确认测试失败**
- [ ] **Green: 最小实现**

每帧 ACK 确认后推进 offset，所有帧完成后触发回调。

- [ ] **Green: 确认测试通过**
- [ ] **Refactor**
- [ ] **Commit**

```bash
git commit -m "feat: long data complete callback with ACK"
```

---

### Task 22: ACK 通过中断方式确认

**Scenario:**
```gherkin
Given 硬件接口的 ACK 方式配置为"中断"
When 发送一帧后硬件触发发送完成中断
Then 实例通过中断回调确认该帧发送成功
```

**文件：**
- 测试: `tests/test_ack.c`
- 实现: `src/comm_route.c`

- [ ] **Red: 编写失败测试**

```c
void test_ack_interrupt_mode(void) {
    // setup: ack_enabled=1, ack_mode=CR_ACK_MODE_INTERRUPT
    complete_called = 0;
    uint8_t data[] = {1, 2, 3};
    cr_send(&inst, 0x03, 0, data, 3, mock_on_complete, NULL);

    cr_poll(&inst);  // 发送
    TEST_ASSERT_EQUAL_INT(0, complete_called);

    cr_notify_send_done(&inst);  // 模拟中断
    cr_poll(&inst);  // 处理标志

    TEST_ASSERT_EQUAL_INT(1, complete_called);
    TEST_ASSERT_EQUAL_UINT8(0, complete_status);
}
```

- [ ] **Red: 确认测试失败**
- [ ] **Green: 最小实现**

实现 `cr_notify_send_done()`：设置标志位。
`cr_poll` 中 WAIT_ACK 状态检查标志位：置位则确认成功。

- [ ] **Green: 确认测试通过**
- [ ] **Refactor**
- [ ] **Commit**

```bash
git commit -m "feat: ACK interrupt mode via cr_notify_send_done"
```

---

### Task 23: 接收端自动发送 ACK 回复

**Scenario:**
```gherkin
# 此 Task 验证接收端收到数据帧后自动回复 ACK 的行为
# 这是 Feature "ACK 通过目标回复确认" 的实现前提
Given 接收端实例配置 ACK=开启, ACK方式=目标回复, 本机地址=0x03
When 收到一帧发给自己的数据帧（SRC=0x01, SEQ=5）
Then 接收端自动构造并发送 ACK 帧（DST=0x01, SRC=0x03, CTL.bit7=1, SEQ=5）
```

**文件：**
- 测试: `tests/test_ack.c`
- 实现: `src/comm_route.c`

- [ ] **Red: 编写失败测试**

```c
void test_receiver_auto_sends_ack(void) {
    // setup: 接收端实例 (addr=0x03, ack_enabled=1, ack_mode=REPLY)
    sent_len = 0;
    // 喂入数据帧: DST=0x03, SRC=0x01, CTL=0x00, SEQ=5, TTL=3, payload
    uint8_t frame[] = {0x03, 0x01, 0x00, 0x05, 0x03, 'H', 'I'};
    cr_feed_frame(&inst, frame, sizeof(frame));

    // 验证自动发送了 ACK: DST=0x01, SRC=0x03, CTL.bit7=1, SEQ=5
    TEST_ASSERT_TRUE(sent_len > 0);
    TEST_ASSERT_EQUAL_UINT8(0x01, sent_buf[0]);  // DST=原始SRC
    TEST_ASSERT_EQUAL_UINT8(0x03, sent_buf[1]);  // SRC=本机
    TEST_ASSERT_BITS(0x80, 0x80, sent_buf[2]);   // CTL.bit7=1
    TEST_ASSERT_EQUAL_UINT8(0x05, sent_buf[3]);  // SEQ匹配
}
```

- [ ] **Red: 确认测试失败**
- [ ] **Green: 最小实现**

在 `cr_feed_frame` 收到 DST==本机的数据帧时，如果 ack_enabled && ack_mode==REPLY，构造并发送 ACK 帧。

- [ ] **Green: 确认测试通过**
- [ ] **Refactor**
- [ ] **Commit**

```bash
git commit -m "feat: receiver auto-sends ACK reply"
```

---

### Task 24: 接收多帧长数据组装

**Scenario:**
```gherkin
Given 实例配置本机地址=0x02
When 依次收到来自 0x01 的 3 帧分片（序号 0,1,2，第3帧带末帧标记）
Then 库内部缓存并组装
And 收齐后触发接收完成回调，将完整数据交给用户
```

**文件：**
- 测试: `tests/test_recv.c`
- 实现: `src/comm_route.c`

- [ ] **Red: 编写失败测试**

```c
void test_receive_multi_frame_assembly(void) {
    // setup: local_addr=0x02, MTU=8, ack_enabled=0
    recv_called = 0;

    // 帧1: DST=0x02, SRC=0x01, CTL=0x20(分片), SEQ=0, TTL=3, payload 8B
    uint8_t f1[] = {0x02, 0x01, 0x20, 0x00, 0x03, 0,1,2,3,4,5,6,7};
    // 帧2: CTL=0x20(分片), SEQ=1
    uint8_t f2[] = {0x02, 0x01, 0x20, 0x01, 0x03, 8,9,10,11,12,13,14,15};
    // 帧3: CTL=0x30(分片+末帧), SEQ=2, payload 4B
    uint8_t f3[] = {0x02, 0x01, 0x30, 0x02, 0x03, 16,17,18,19};

    cr_feed_frame(&inst, f1, sizeof(f1));
    TEST_ASSERT_EQUAL_INT(0, recv_called);
    cr_feed_frame(&inst, f2, sizeof(f2));
    TEST_ASSERT_EQUAL_INT(0, recv_called);
    cr_feed_frame(&inst, f3, sizeof(f3));
    TEST_ASSERT_EQUAL_INT(1, recv_called);
    TEST_ASSERT_EQUAL_UINT16(20, recv_len);

    uint8_t expected[20];
    for (int i = 0; i < 20; i++) expected[i] = i;
    TEST_ASSERT_EQUAL_UINT8_ARRAY(expected, recv_data, 20);
}
```

- [ ] **Red: 确认测试失败**
- [ ] **Green: 最小实现**

实现 RX 组装逻辑：
- 分片帧 → 按 SRC+BizID 找/创建组装槽
- 写入 payload 到组装缓冲区
- 末帧标记 → 触发回调，释放槽位

- [ ] **Green: 确认测试通过**
- [ ] **Refactor**
- [ ] **Commit**

```bash
git commit -m "feat: multi-frame receive assembly"
```

---

### Task 25: 接收单帧短数据

**Scenario:**
```gherkin
Given 实例配置本机地址=0x02
When 收到来自 0x01 的单帧数据（非分片标记）
Then 直接触发接收回调，将数据交给用户
```

**文件：**
- 测试: `tests/test_recv.c`
- 实现: 已在 Task 8 实现（验证性测试）

- [ ] **Red: 编写失败测试**（确认 Task 8 已覆盖）
- [ ] **Green: 确认测试通过**
- [ ] **Refactor:** 运行全部测试确认不破坏，审视是否有重复 setup 可提取为 helper。
- [ ] **Commit**

```bash
git commit -m "test: confirm single frame receive (already implemented)"
```

---

### Task 26: 组装超时释放槽位

**Scenario:**
```gherkin
Given 实例配置本机地址=0x02, rx_assem_timeout_ms=500
And 正在组装来自 0x01 的长数据（已收到序号 0,1）
When 超过组装超时时间仍未收到后续帧
Then 释放该 RX 组装槽位
And 已缓存的不完整数据被丢弃
```

**文件：**
- 测试: `tests/test_recv.c`
- 实现: `src/comm_route.c`

- [ ] **Red: 编写失败测试**

```c
void test_rx_assembly_timeout(void) {
    // setup: rx_assem_timeout_ms=500
    // 喂入帧1和帧2（分片，非末帧）
    uint8_t f1[] = {0x02, 0x01, 0x20, 0x00, 0x03, 1,2,3,4,5,6,7,8};
    uint8_t f2[] = {0x02, 0x01, 0x20, 0x01, 0x03, 9,10,11,12,13,14,15,16};
    cr_feed_frame(&inst, f1, sizeof(f1));
    cr_feed_frame(&inst, f2, sizeof(f2));

    // 推进时间超过超时
    mock_tick = 600;
    cr_poll(&inst);

    // 再喂入帧3（末帧），应该不会触发回调（槽已释放）
    recv_called = 0;
    uint8_t f3[] = {0x02, 0x01, 0x30, 0x02, 0x03, 17,18,19,20};
    cr_feed_frame(&inst, f3, sizeof(f3));
    TEST_ASSERT_EQUAL_INT(0, recv_called);  // 槽已释放，帧3作为新分片首帧（SEQ≠0）被丢弃或创建新槽
}
```

- [ ] **Red: 确认测试失败**
- [ ] **Green: 最小实现**

在 `cr_poll` 中遍历 RX 组装槽，检查 `get_tick_ms() - last_active > rx_assem_timeout_ms` → 清零释放。

- [ ] **Green: 确认测试通过**
- [ ] **Refactor**
- [ ] **Commit**

```bash
git commit -m "feat: RX assembly timeout releases slot"
```

---

### Task 27: 组装槽满时丢弃新长数据首帧

**Scenario:**
```gherkin
Given 实例配置 rx_assem_count=2
And 2 个组装槽均被占用
When 收到来自 0x04 的新长数据首帧（序号=0）
Then 该帧被丢弃（无可用槽位）
```

**文件：**
- 测试: `tests/test_recv.c`
- 实现: `src/comm_route.c`

- [ ] **Red: 编写失败测试**

```c
void test_rx_slot_full_drops_new(void) {
    // setup: rx_assem_count=2
    // 占满2个槽
    uint8_t f_a[] = {0x02, 0x01, 0x20, 0x00, 0x03, 1,2,3};  // from 0x01
    uint8_t f_b[] = {0x02, 0x03, 0x20, 0x00, 0x03, 4,5,6};  // from 0x03
    cr_feed_frame(&inst, f_a, sizeof(f_a));
    cr_feed_frame(&inst, f_b, sizeof(f_b));

    // 第三个源来的首帧
    recv_called = 0;
    uint8_t f_c[] = {0x02, 0x04, 0x20, 0x00, 0x03, 7,8,9};  // from 0x04
    cr_feed_frame(&inst, f_c, sizeof(f_c));

    // 验证未创建新槽（被丢弃）
    TEST_ASSERT_EQUAL_INT(0, recv_called);
}
```

- [ ] **Red: 确认测试失败**
- [ ] **Green: 最小实现**

分配组装槽时，无空闲槽则返回（丢弃帧）。

- [ ] **Green: 确认测试通过**
- [ ] **Refactor**
- [ ] **Commit**

```bash
git commit -m "feat: drop new long data when RX slots full"
```

---

### Task 28: 重复帧不重复写入

**Scenario:**
```gherkin
Given 正在组装来自 0x01 的长数据，已收到序号 0,1
When 再次收到来自 0x01 的序号=1 的帧（重传帧）
Then 不重复写入组装缓冲区
And 仍发送 ACK 确认
```

**文件：**
- 测试: `tests/test_recv.c`
- 实现: `src/comm_route.c`

- [ ] **Red: 编写失败测试**

```c
void test_rx_duplicate_frame_ignored(void) {
    // setup: ack_enabled=1, ack_mode=REPLY
    uint8_t f0[] = {0x02, 0x01, 0x20, 0x00, 0x03, 1,2,3,4,5,6,7,8};
    uint8_t f1[] = {0x02, 0x01, 0x20, 0x01, 0x03, 9,10,11,12,13,14,15,16};
    cr_feed_frame(&inst, f0, sizeof(f0));
    cr_feed_frame(&inst, f1, sizeof(f1));

    // 重复帧1
    send_count = 0;  // reset send counter
    cr_feed_frame(&inst, f1, sizeof(f1));

    // 应发送ACK
    TEST_ASSERT_TRUE(send_count > 0);

    // 发末帧验证数据正确（未被重复帧破坏）
    recv_called = 0;
    uint8_t f2[] = {0x02, 0x01, 0x30, 0x02, 0x03, 17,18,19,20};
    cr_feed_frame(&inst, f2, sizeof(f2));
    TEST_ASSERT_EQUAL_INT(1, recv_called);
    TEST_ASSERT_EQUAL_UINT16(20, recv_len);
}
```

- [ ] **Red: 确认测试失败**
- [ ] **Green: 最小实现**

组装槽检查 SEQ ≤ expected_seq - 1（已收到的）则跳过写入，但仍回 ACK。

- [ ] **Green: 确认测试通过**
- [ ] **Refactor**
- [ ] **Commit**

```bash
git commit -m "feat: ignore duplicate frames in RX assembly, still ACK"
```

---

### Task 29: 广播与单播并行

**Scenario:**
```gherkin
Given 有一个正在发送的单播长数据任务（3帧，发到第2帧）
When 用户提交一个广播单帧发送请求
Then 广播帧可以在下一次 poll 中发出
And 单播长数据任务继续推进不受影响
```

**文件：**
- 测试: `tests/test_broadcast.c`
- 实现: `src/comm_route.c`

- [ ] **Red: 编写失败测试**

```c
void test_broadcast_parallel_with_unicast(void) {
    // setup: MTU=8, ack_enabled=0, frame_interval=0
    send_count = 0;
    uint8_t data[20] = {0};
    cr_send(&inst, 0x03, 0, data, 20, NULL, NULL);

    cr_poll(&inst);  // 单播帧1
    cr_poll(&inst);  // 单播帧2
    TEST_ASSERT_EQUAL_INT(2, send_count);

    // 提交广播
    uint8_t bcast[] = "BC";
    cr_broadcast(&inst, 1, bcast, 2);

    cr_poll(&inst);  // 应发出广播帧 + 单播帧3（或至少广播帧）
    // 验证广播帧被发出
    int found_broadcast = 0;
    for (int i = 2; i < send_count; i++) {
        if (send_history[i][0] == 0xFF) found_broadcast = 1;
    }
    TEST_ASSERT_EQUAL_INT(1, found_broadcast);
}
```

- [ ] **Red: 确认测试失败**
- [ ] **Green: 最小实现**

`cr_poll` 中分别推进单播槽和广播槽，两者独立。

- [ ] **Green: 确认测试通过**
- [ ] **Refactor**
- [ ] **Commit**

```bash
git commit -m "feat: broadcast and unicast parallel send"
```

---

### Task 30: 三节点链式拓扑 - ACK 经中间节点回传

**Scenario:**
```gherkin
Given 节点 A 地址=0x01, 路由表：0x03→下一跳0x02
And 节点 B 地址=0x02, 路由表：0x01→直连, 0x03→直连
And 节点 C 地址=0x03, 路由表：0x01→下一跳0x02
When A 通过 B 发送数据帧到 C
And C 收到数据帧后自动发送 ACK（DST=0x01, SRC=0x03）
Then B 收到该 ACK 帧，DST=0x01≠本机，查路由表转发到 A
And A 收到 ACK 帧，DST=0x01==本机，匹配 SEQ 标记成功
```

**文件：**
- 测试: `tests/test_multihop.c`
- 实现: 已有逻辑（集成验证）

- [ ] **Red: 编写失败测试**

```c
// 三套独立 mock HAL
static uint8_t sent_buf_a[64], sent_buf_b[64], sent_buf_c[64];
static uint16_t sent_len_a, sent_len_b, sent_len_c;
static int mock_send_node_a(void *ctx, const uint8_t *data, uint16_t len) {
    memcpy(sent_buf_a, data, len); sent_len_a = len; return 0;
}
static int mock_send_node_b(void *ctx, const uint8_t *data, uint16_t len) {
    memcpy(sent_buf_b, data, len); sent_len_b = len; return 0;
}
static int mock_send_node_c(void *ctx, const uint8_t *data, uint16_t len) {
    memcpy(sent_buf_c, data, len); sent_len_c = len; return 0;
}
static uint32_t tick_val = 0;
static uint32_t get_tick(void) { return tick_val; }

static uint8_t complete_status_mh;
static int complete_called_mh;
static void on_complete_mh(uint8_t status, void *ctx) {
    complete_status_mh = status; complete_called_mh = 1;
}

void test_multihop_ack_routing(void) {
    // --- 节点 A (0x01) ---
    uint8_t buf_a[1024];
    cr_route_entry_t routes_a[] = { {.dest = 0x03, .next_hop = 0x02} };
    cr_config_t cfg_a = { .local_addr = 0x01, .mtu = 32, .frame_interval_ms = 0,
        .max_retries = 3, .ack_timeout_ms = 100, .ack_enabled = 1,
        .ack_mode = CR_ACK_MODE_REPLY, .default_ttl = 3,
        .tx_queue_depth = 4, .rx_assem_count = 2, .dedup_table_size = 8,
        .rx_assem_timeout_ms = 1000, .route_table = routes_a, .route_count = 1 };
    cr_instance_t inst_a;
    cr_init(&inst_a, &cfg_a, buf_a, sizeof(buf_a));
    cr_hal_t hal_a = { .send = mock_send_node_a, .get_tick_ms = get_tick };
    cr_set_hal(&inst_a, &hal_a);

    // --- 节点 B (0x02) ---
    uint8_t buf_b[1024];
    cr_route_entry_t routes_b[] = { {.dest = 0x01, .next_hop = 0x01},
                                     {.dest = 0x03, .next_hop = 0x03} };
    cr_config_t cfg_b = { .local_addr = 0x02, .mtu = 32, .frame_interval_ms = 0,
        .max_retries = 3, .ack_timeout_ms = 100, .ack_enabled = 1,
        .ack_mode = CR_ACK_MODE_REPLY, .default_ttl = 3,
        .tx_queue_depth = 4, .rx_assem_count = 2, .dedup_table_size = 8,
        .rx_assem_timeout_ms = 1000, .route_table = routes_b, .route_count = 2 };
    cr_instance_t inst_b;
    cr_init(&inst_b, &cfg_b, buf_b, sizeof(buf_b));
    cr_hal_t hal_b = { .send = mock_send_node_b, .get_tick_ms = get_tick };
    cr_set_hal(&inst_b, &hal_b);

    // --- 节点 C (0x03) ---
    uint8_t buf_c[1024];
    cr_route_entry_t routes_c[] = { {.dest = 0x01, .next_hop = 0x02} };
    cr_config_t cfg_c = { .local_addr = 0x03, .mtu = 32, .frame_interval_ms = 0,
        .max_retries = 3, .ack_timeout_ms = 100, .ack_enabled = 1,
        .ack_mode = CR_ACK_MODE_REPLY, .default_ttl = 3,
        .tx_queue_depth = 4, .rx_assem_count = 2, .dedup_table_size = 8,
        .rx_assem_timeout_ms = 1000, .route_table = routes_c, .route_count = 1 };
    cr_instance_t inst_c;
    cr_init(&inst_c, &cfg_c, buf_c, sizeof(buf_c));
    cr_hal_t hal_c = { .send = mock_send_node_c, .get_tick_ms = get_tick };
    cr_set_hal(&inst_c, &hal_c);
    cr_set_recv_callback(&inst_c, NULL, NULL);  // C 不需要接收回调

    // --- 数据流 ---
    complete_called_mh = 0;
    uint8_t data[] = {0xAA, 0xBB, 0xCC};
    cr_send(&inst_a, 0x03, 0, data, 3, on_complete_mh, NULL);
    cr_poll(&inst_a);  // A 发出帧 → sent_buf_a

    // B 收到 A 的帧，DST=0x03≠本机，转发
    sent_len_b = 0;
    cr_feed_frame(&inst_b, sent_buf_a, sent_len_a);
    TEST_ASSERT_TRUE(sent_len_b > 0);  // B 转发了

    // C 收到 B 转发的帧，DST=0x03==本机，自动发 ACK
    sent_len_c = 0;
    cr_feed_frame(&inst_c, sent_buf_b, sent_len_b);
    TEST_ASSERT_TRUE(sent_len_c > 0);  // C 发了 ACK
    TEST_ASSERT_EQUAL_UINT8(0x01, sent_buf_c[0]);  // ACK DST=A
    TEST_ASSERT_BITS(0x80, 0x80, sent_buf_c[2]);   // CTL.bit7=1 (ACK)

    // B 收到 C 的 ACK，DST=0x01≠本机，转发
    sent_len_b = 0;
    cr_feed_frame(&inst_b, sent_buf_c, sent_len_c);
    TEST_ASSERT_TRUE(sent_len_b > 0);  // B 转发 ACK

    // A 收到 ACK
    cr_feed_frame(&inst_a, sent_buf_b, sent_len_b);
    TEST_ASSERT_EQUAL_INT(1, complete_called_mh);
    TEST_ASSERT_EQUAL_UINT8(0, complete_status_mh);  // 成功
}
```

- [ ] **Red: 确认测试失败**
- [ ] **Green: 确认测试通过**（利用已实现的转发+ACK逻辑）
- [ ] **Refactor**
- [ ] **Commit**

```bash
git commit -m "test: multi-hop ACK routing integration test"
```

---

### Task 31: 中间节点不消费非自己的 ACK 帧

**Scenario:**
```gherkin
Given 节点 B 地址=0x02
And B 有一个活跃发送任务（SEQ=5, 目标=0x04）
When B 收到 ACK 帧（DST=0x01, SRC=0x03, SEQ=5）
Then B 不匹配该 ACK（DST≠本机），按路由表转发
```

**文件：**
- 测试: `tests/test_multihop.c`
- 实现: 已有逻辑（验证）

- [ ] **Red: 编写失败测试**

```c
void test_intermediate_node_does_not_consume_foreign_ack(void) {
    // B 有活跃任务 SEQ=5
    cr_send(&inst_b, 0x04, 0, data, 3, mock_on_complete, NULL);
    cr_poll(&inst_b);

    // 收到一个目标不是B的ACK帧
    complete_called = 0;
    sent_len = 0;
    uint8_t ack[] = {0x01, 0x03, 0x80, 0x05, 0x03};  // DST=0x01
    cr_feed_frame(&inst_b, ack, sizeof(ack));

    // B不应消费这个ACK
    TEST_ASSERT_EQUAL_INT(0, complete_called);
    // B应转发
    TEST_ASSERT_TRUE(sent_len > 0);
}
```

- [ ] **Red: 确认测试失败**
- [ ] **Green: 确认测试通过**（DST-first 逻辑已保证）
- [ ] **Refactor**
- [ ] **Commit**

```bash
git commit -m "test: intermediate node forwards foreign ACK"
```

---

### Task 32: 星型拓扑集成测试

**Scenario:**
```gherkin
Given 中心节点地址=0x01
And 路由表：0x02→直连, 0x03→直连, 0x04→直连
When 收到来自 0x02 发给 0x03 的帧
Then 中心节点转发该帧到 0x03
```

**文件：**
- 测试: `tests/test_multihop.c`

- [ ] **Red: 编写失败测试**

```c
void test_star_topology_routing(void) {
    // 中心节点 0x01, route: 0x02→直连, 0x03→直连
    sent_len = 0;
    uint8_t frame[] = {0x03, 0x02, 0x00, 0x00, 0x03, 'D'};
    cr_feed_frame(&inst, frame, sizeof(frame));

    TEST_ASSERT_TRUE(sent_len > 0);
    TEST_ASSERT_EQUAL_UINT8_ARRAY(frame, sent_buf, sizeof(frame));
}
```

- [ ] **Red: 确认测试失败**
- [ ] **Green: 确认测试通过**（已有转发逻辑）
- [ ] **Refactor:** 运行全部测试确认不破坏。
- [ ] **Commit**

```bash
git commit -m "test: star topology routing"
```

---

### Task 33: calc_buffer_size 正确性

**验证 `cr_calc_buffer_size` 返回值与实际需求一致。**

**文件：**
- 测试: `tests/test_instance.c`

- [ ] **Red: 编写失败测试**

```c
void test_calc_buffer_size(void) {
    cr_config_t cfg = {
        .mtu = 64, .tx_queue_depth = 4,
        .rx_assem_count = 2, .dedup_table_size = 16,
    };
    size_t size = cr_calc_buffer_size(&cfg);
    // 能用这个size初始化成功
    uint8_t *buf = (uint8_t *)malloc(size);  // 测试中允许malloc
    cr_instance_t inst;
    TEST_ASSERT_EQUAL_INT(0, cr_init(&inst, &cfg, buf, size));
    // 少一字节则失败
    TEST_ASSERT_EQUAL_INT(-3, cr_init(&inst, &cfg, buf, size - 1));
    free(buf);
}
```

- [ ] **Red: 确认测试失败**
- [ ] **Green: 确认测试通过**
- [ ] **Refactor:** 运行全部测试确认不破坏。
- [ ] **Commit**

```bash
git commit -m "test: verify calc_buffer_size accuracy"
```

---

## 任务依赖图

```
Task 0 (脚手架)
  └─ Task 1 (创建实例)
      ├─ Task 2 (buffer不足)
      ├─ Task 3 (多实例隔离)
      └─ Task 4 (单播发送)
          ├─ Task 5 (队列满)
          ├─ Task 6 (广播发送)
          │   ├─ Task 7 (广播超MTU)
          │   ├─ Task 11 (广播转发)
          │   │   ├─ Task 12 (TTL耗尽)
          │   │   └─ Task 13 (去重)
          │   └─ Task 29 (广播单播并行)
          ├─ Task 8 (接收单帧)
          │   ├─ Task 9 (转发)
          │   │   ├─ Task 10 (无路由丢弃)
          │   │   └─ Task 32 (星型拓扑，依赖转发)
          │   └─ Task 24 (多帧组装)
          │       ├─ Task 25 (单帧接收确认)
          │       ├─ Task 26 (组装超时)
          │       ├─ Task 27 (槽满丢弃)
          │       └─ Task 28 (重复帧)
          ├─ Task 14 (长数据拆帧)
          │   ├─ Task 15 (帧间隔)
          │   └─ Task 17 (完成回调ACK关)
          ├─ Task 16 (ACK关闭)
          └─ Task 18 (ACK正常确认)
              ├─ Task 19 (超时重传)
              │   └─ Task 20 (最大重传)
              ├─ Task 21 (长数据+ACK回调)
              ├─ Task 22 (中断ACK)
              └─ Task 23 (接收端自动回ACK)
                  ├─ Task 30 (多跳ACK集成，依赖 Task 9 + Task 23)
                  └─ Task 31 (中间节点不消费ACK)
Task 33 (calc_buffer_size) ← 依赖 Task 1
```
